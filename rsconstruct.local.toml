# Repo-specific overlay. Deep-merged over the canonical rsconstruct.toml.

# Templates live in tera.templates/ and render to the repo root. Configs are
# Lua (config/*.lua), covered by the canonical dep_auto list; load_lua() also
# content-tracks each file it reads. platform.lua is not in the canonical
# dep_auto list, so it is added here.
[processor.tera]
src_dirs = ["tera.templates"]
dep_inputs = [
	"config/platform.lua",
]

# requirements.txt is not generated from import scanning; pyproject.toml
# declares the dependencies.
[processor.requirements]
enabled = false

# Runtime deps so CI (rsconstruct tools install-deps) can run pytest.
[dependencies]
pip = [
    "gitpython",
    "jsonschema",
    "lupa",
    "mako",
    "pyfakeuse",
    "pylogconf",
    "pytconf",
    "pyyaml",
    "sphinx",
    "venv-run",
    "types-jsonschema",
    "types-PyYAML",
]
